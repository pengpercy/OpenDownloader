.DS_Store
.idea/
.vscode/
*.user
*.suo
*.userprefs
*.usertasks
*.userosscache
*.sln.docstates

# Build results
[Bb]in/
[Oo]bj/
build/
dist/
artifacts/

# Nuget
*.nupkg
*.snupkg
project.lock.json
project.fragment.lock.json
artifacts/

# Generated files
src/OpenDownloader/Assets/Binaries/
!src/OpenDownloader/Assets/Binaries/.gitkeep
# Keep the binaries we manually added if they are not generated by build
# Actually, we put binaries in Assets/Binaries manually, so we should NOT ignore them 
# unless they are copied there by build. 
# In this project, they seem to be source files.
# Let's un-ignore them.
!src/OpenDownloader/Assets/Binaries/**

# Aria2 config/session in dev
aria2.conf
aria2.session
aria2.log
